[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
# Show locals in tracebacks for easier debugging
addopts = --tb=short -q
# Fail on first error during development (override with -x)
