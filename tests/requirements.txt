# Simple Photos — Python test dependencies
#
# Install with:
#     python3 -m venv .venv
#     . .venv/bin/activate
#     pip install -r tests/requirements.txt
#
# Versions are PINNED to exact releases for reproducible CI/release builds.
# Bumping a pin should be a deliberate, reviewed change — package upgrades have
# repeatedly produced behavioural drift that is hard to debug after the fact.
# Re-pin by running `pip freeze` in a clean venv after upgrading.

pytest==9.0.3
requests==2.33.1
Pillow==12.2.0           # image generation (generate_test_jpeg, etc.); 12.2 fixes CVE-2026-25990/40192/42308-42311
cryptography==48.0.0     # TLS / AESGCM tests (test_22, test_26)
numpy==2.4.4             # AI accuracy tests (vector ops in test_59)
piexif==1.1.3            # EXIF portrait orientation tests (test_25, test_27)
