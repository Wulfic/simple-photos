# Simple Photos Web — Environment Variables
# Copy this to .env and customise.
# .env is gitignored and must NEVER be committed to version control.

# API base URL (leave blank to use relative /api — recommended for production)
VITE_API_BASE_URL=

# Set to "true" to enable verbose console logging in production builds
VITE_DEBUG=false
