/**
 * Persistent progress banners for long-running background tasks.
 *
 * Previously showed conversion progress banners. With conversion removed,
 * this component is a no-op placeholder. Can be extended for future
 * background task UI (e.g. bulk operations).
 */

export default function GlobalProgressBanners() {
  return null;
}
